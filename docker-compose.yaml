version: "3"
services:
  crabi-api:
    build:
      context: .
    ports:
      - "8080:8080"
    depends_on:
      - pld-container

  pld-container:
    image: vligascrabi/crabi-pld-test:v1
    ports:
      - "3000:3000"
